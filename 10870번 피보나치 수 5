#include <iostream>
#include <algorithm>
using namespace std;
int fibo(int num) {
	if (num == 0) {
		return 0;
	}
	else if (num == 1) {
		return 1;
	}
	else {
		return fibo(num - 2) + fibo(num - 1);
	}
}
int main() {
	int n;
	cin >> n;
	cout << fibo(n) << endl;
}
