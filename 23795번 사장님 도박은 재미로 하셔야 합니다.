#include <iostream>
using namespace std;
int main(){
    int sum = 0;
    while (true){
        int k;
        cin>>k;
        if (k == -1) {
            break;
        }
        sum += k;
    }
    cout << sum <<'\n';
}
