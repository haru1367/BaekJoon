#include <iostream>
using namespace std;
int main() {
	int N;
	cin >> N;
	int alpha = N;
	int num = 0;
	while (true) {
		N = (N % 10) * 10 + ((N / 10) + (N % 10)) % 10;
		if (N == alpha) {
			break;
		}
		else {
			num++;
		}
	}
	num++;
	cout << num << endl;
}
