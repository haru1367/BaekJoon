#include <iostream>
#include <string>
#include <sstream>
#include <algorithm>
using namespace std;
int main() {
	string str;
	getline(cin, str);
	int num = 0;
	stringstream ss(str);
	string s1 = "";
	while (ss >> s1) {
		num++;
		
	}
	cout << num << endl;
}
