#include <iostream>
#include <string>
using namespace std;
int main() {
	int A, B;
	cin >> A >> B;
	string stra = to_string(A);
	string strb = to_string(B);
	string resulta = "";
	string resultb = "";
	for (int i = stra.size()-1; i >=0; i--) {
		resulta += stra[i];
		resultb += strb[i];
	}
	if (stoi(resulta) > stoi(resultb)) {
		cout << resulta << endl;
	}
	else {
		cout << resultb << endl;
	}
}
