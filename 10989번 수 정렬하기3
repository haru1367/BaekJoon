#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;
int arr[10001] = { 0 };
int main() {
	int N;
	cin >> N;
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	for (int i = 0; i < N; i++) {
		int k;
		cin >> k;
		arr[k]++;
	}
	for (int i = 1; i <= 10000; i++) {
		for (int j = 0; j < arr[i]; j++) {
			cout << i << '\n';
		}
	}
}
