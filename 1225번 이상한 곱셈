#include <iostream>
#include <string>
using namespace std;
int main() {
	string A, B;
	cin >> A >> B;
	long long sum = 0;
	for (int i = 0; i < A.size(); i++) {
		for (int j = 0; j < B.size(); j++) {
			sum += (A[i] - 48) * (B[j] - 48);
		}
	}
	cout << sum << '\n';
}
