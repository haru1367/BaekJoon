#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;
int main() {
	int x, y, w, h;
	cin >> x >> y >> w >> h;
	int a = w - x;
	int b = h - y;
	int a1 = min(x, y);
	int b1 = min(a, b);
	cout << min(a1, b1) << '\n';
}
