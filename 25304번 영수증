#include <iostream>
#include <algorithm>
using namespace std;
int main() {
	int X;
	cin >> X;
	int N;
	cin >> N;
	for (int i = 1; i <= N; i++) {
		int a, b;
		cin >> a >> b;
		X -= a * b;
	}
	if (X == 0) {
		cout << "Yes" << '\n';
	}
	else {
		cout << "No" << '\n';
	}
}
