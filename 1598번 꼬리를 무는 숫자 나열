#include <iostream>
#include <cmath>
using namespace std;
int main() {
	int N, M;
	cin >> N >> M;
	int x1 = N % 4;
	if (x1 == 0) x1 = 4;
	int y1 = (N - 1) / 4;
	int x2 = M % 4;
	if (x2 == 0) x2 = 4;
	int y2 = (M - 1) / 4;
	cout << abs(x1 - x2) + abs(y1 - y2);
}
